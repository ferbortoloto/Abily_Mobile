/**
 * @deprecated Use o componente ImagePickerSheet em vez desta função.
 * Alert.alert no Android pode causar perda do resultado da Activity do picker.
 * Este arquivo é mantido apenas para compatibilidade durante a migração.
 */
export function showImagePickerAlert() {
  // Não faz nada — todos os callers foram migrados para ImagePickerSheet
}
